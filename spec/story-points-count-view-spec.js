'use babel';

import StoryPointsCountView from '../lib/story-points-count-view';

describe('StoryPointsCountView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
